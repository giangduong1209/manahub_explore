import Icon from '@ant-design/icons';

import { ReactComponent as dexIcon } from './svgs/dexIcon.svg';

const DexLogo = (props) => {
  return <Icon component={dexIcon} {...props} />;
};

export default DexLogo;
