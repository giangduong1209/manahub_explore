import Icon from '@ant-design/icons';

import { ReactComponent as Playstation } from './svgs/playstation.svg';

const PlaystationIcon = (props) => {
  return <Icon component={Playstation} {...props} />;
};

export default PlaystationIcon;
