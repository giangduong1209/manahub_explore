import Icon from '@ant-design/icons';

import { ReactComponent as SiteMap } from './svgs/sitemapIcon.svg';

const SiteMapIcon = (props) => {
  return <Icon component={SiteMap} {...props} />;
};

export default SiteMapIcon;
