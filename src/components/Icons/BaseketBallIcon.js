import Icon from '@ant-design/icons';

import { ReactComponent as Ball } from './svgs/baseketball.svg';

const BaseketBallIcon = (props) => {
  return <Icon component={Ball} {...props} />;
};

export default BaseketBallIcon;
