import Icon from '@ant-design/icons';

import { ReactComponent as Female } from './svgs/female.svg';

const FemaleIcon = (props) => {
  return <Icon component={Female} {...props} />;
};

export default FemaleIcon;
